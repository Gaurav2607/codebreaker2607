/**
 * Array at method
 */

let arr = [2,3,4,5];

console.log('first array value',arr[1]);

console.log('last array', arr[arr.length - 1]);

let firstArray = arr.at(1);
console.log(firstArray);

let lastArray = arr.at(-1);
console.log(lastArray);

